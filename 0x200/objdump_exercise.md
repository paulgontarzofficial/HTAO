### Objdump

We are using `objdump` to view the assembly of this program. 

```objdump -D firstprog | grep -A20 main.:```

The above command shows us the main functions assembly code that we can start
taking a deeper look at. 

